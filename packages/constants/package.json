{
  "name": "@cp-checklist/constants",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "format": "eslint src --fix"
  },
  "devDependencies": {
    "@cp-checklist/config": "workspace:0.0.0"
  }
}